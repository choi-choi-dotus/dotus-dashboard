# Dotus 매출 대시보드

## 포함 파일
- `app.py` — 대시보드 메인 코드
- `data.xlsx` — 매출/입고 데이터
- `requirements.txt` — 필요한 라이브러리 목록
- `.streamlit/config.toml` — 화면 테마 설정

## 배포 방법 (Streamlit Cloud)

1. GitHub에 이 폴더 전체를 업로드
2. https://streamlit.io/cloud 접속 → 구글 로그인
3. "New app" 클릭 → GitHub 저장소 선택
4. Main file: `app.py` 선택 → Deploy

## 로그인 비밀번호
`dotus2026`

비밀번호 변경은 app.py 상단 `PASSWORD = "dotus2026"` 수정
